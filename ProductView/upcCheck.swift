/*import Foundation
import Cocoa

class UpcCheck {

    
    var upcScan : String
    
init() {
    // palceholder variable for where the upc will be scanned into the UI
    //self.upcScan = "888380730546"
      self.upcScan = upcScanIn
    
      //self.upcScan = "801682090309"
}


func upcCheck() -> String {
var tempUPC: String
            let filteredUpc = products.filter({ return $0.upc == upcScan})
            tempUPC = filteredUpc[0].upc
            print("UPC: \(filteredUpc[0].upc)")
            print("Color: \(filteredUpc[0].color)")
            print("Original Qty: \(filteredUpc[0].originalQty)")
            print("scanCount before scan: \(filteredUpc[0].scanCount)")
            filteredUpc[0].scanCount += 1
            print("After scan the scanCount has been updated to: \(filteredUpc[0].scanCount)")
            //filteredUpc[0].originalQty += 1
            //print(filteredUpc[0].originalQty)
    return tempUPC
            
            
        
//var filtered = products.filter  {$0.upc.contains(upcScan) }
           // if filtered.count > 0 {
                //for i in 0...filtered.count - 1 {
                    //do Stuff
                    
                //increment counter for UPC
            //dump(filtered)
              // print(products.filter.upc)
                         //   } else {
               // print("no matches for UPC \(upcScan)")
}

}


*/


