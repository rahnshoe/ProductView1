/*import Cocoa

public class AddWomens {
    var womens: String
    
    
    public init() {
        self.womens = "Womens "
    }
 
    
    public func addWomens() {
        var filtered = products.filter  {$0.itemDescription.contains("Hudson") }
        if filtered.count > 0 {
            for i in 0...filtered.count - 1 {
                filtered[i].itemDescription.insert(contentsOf: womens, at: filtered[i].itemDescription.index(filtered[i].itemDescription.startIndex, offsetBy: 13))
                print(filtered[i].itemDescription)
                //dump(products)
            }
        } else {
            print("no matches for Hudson")
        }
        filtered = products.filter { $0.itemDescription.contains("GUESS") }
        if filtered.count > 0 {
            for i in 0...filtered.count - 1 {
                filtered[i].itemDescription.insert(contentsOf: womens, at: filtered[i].itemDescription.index(filtered[i].itemDescription.startIndex, offsetBy: 6))
                print(filtered[i].itemDescription)
                //dump(products)
            }
        } else {
            print("no matches for GUESS")
        }
        filtered = products.filter { $0.itemDescription.contains("Free People") }
        if filtered.count > 0 {
           for i in 0...filtered.count - 1 {
                filtered[i].itemDescription.insert(contentsOf: womens, at: filtered[i].itemDescription.index(filtered[i].itemDescription.startIndex, offsetBy: 12))
                print(filtered[i].itemDescription)
                //dump(products)
            }
        } else {
            print("no matches for Free People")
        }
        filtered = products.filter { $0.itemDescription.contains("Fart") }
        if filtered.count > 0 {
            for i in 0...filtered.count - 1 {
                filtered[i].itemDescription.insert(contentsOf: womens, at: filtered[i].itemDescription.index(filtered[i].itemDescription.startIndex, offsetBy: 6))
                print(filtered[i].itemDescription)
                //dump(products)
            }
        } else {
            print("no matches for FART")
        }
    
    
    
    
    
    
}
}

*/



